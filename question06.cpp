#include<bits/stdc++.h>
using namespace std;
int main()
{
  map<string,int>x;
  x.empty()?printf("map is empty"):printf("map is not empty");
 cout<<endl;
  x["a"]=1;
  x["zoya"]=2;
  x["shyam"]=3;
  x["ram"]=5;
  x["soha"]=0; 

  x.empty()?printf("map is empty"):printf("map is not empty");

}